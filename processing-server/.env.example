# Example environment for processing-server
# Use a strong passphrase; this is read locally by the server
MASTER_PASSWORD=change_me
# HOST defaults to 127.0.0.1; set to 0.0.0.0 only if you understand the risks
# HOST=127.0.0.1
# PORT=8080
